# Backend runtime examples. Export these in your process manager, systemd unit, or shell.

APP_ENV=production
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8010
BACKEND_WORKERS=2

SYSLOG_RECEIVER_HOST=0.0.0.0
SYSLOG_RECEIVER_PORT=1514
TRAP_RECEIVER_HOST=0.0.0.0
TRAP_RECEIVER_PORT=1162

LLM_BASE_URL=https://api.example.com/v1
LLM_MODEL=your-model
LLM_API_KEY=replace-me

