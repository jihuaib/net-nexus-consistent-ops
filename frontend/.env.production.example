# Production option A, recommended:
# leave VITE_API_BASE unset and serve frontend + /api behind the same reverse proxy.

# Production option B:
# build with an explicit API origin when frontend and backend are on different domains.
# VITE_API_BASE=https://api.example.com

